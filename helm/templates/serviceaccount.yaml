{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "selfcare-ms-product.serviceAccountName" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "selfcare-ms-product.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
