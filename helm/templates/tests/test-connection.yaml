apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "selfcare-ms-product.fullname" . }}-test-connection"
  labels:
    {{- include "selfcare-ms-product.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "selfcare-ms-product.fullname" . }}:{{ .Values.service.port }}/actuator/health/liveness']
  restartPolicy: Never
