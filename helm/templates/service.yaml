apiVersion: v1
kind: Service
metadata:
  name: {{ include "selfcare-ms-product.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "selfcare-ms-product.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "selfcare-ms-product.selectorLabels" . | nindent 4 }}
